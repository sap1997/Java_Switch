EMPLOYEE:- ID PRIMARY,TRANSACTION,TIMESTAMP

SELECT * FROM EMPLOYEE WHERE TODATE(TIMESTAMP)<= SYSDATE-30 HAVING
 COUNT( SELECT UNIQUE(TRANSACTION) FROM EMPLOYEE)=15;


EMPLOYEE:- ID,TRANSACTION,TIMESTAMP


ADD COLOUMN NEWLYADDED 
INTO TABLE EMPLOYEE;

UPDATE EMPLOYEE
SET NEWLYADDED = 'OLD'
WHERE ID IS NOT NULL;

