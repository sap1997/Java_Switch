String a:- Hello;
String b:- World;

void stringswap()
{
      a = a.concat(b)//HelloWorld; //a length = 10   //b length = 5
      b = a.substring(0,a.length()-b.length()); //Hello
      a = a.substring(b.length(),a.length()); //World;

}


Student:- age,gender,rollno;

List<Student> student = Arrays.asList;


Map<String,Integer> studentgendercount = student.stream().collect(Collectors.groupingBy(Student::getGender,Collectors.counting());// MALE - 1	FEMALE -1					


@RestController
public class RestCOntroller
{

@Autowired
private UserService UserService;

@GetMapping("/users/"{id})
private ResponseEntity<Users> getUserById(@PathVariable Long ID)
{
	return 	UserService.getUserById(ID);

}

@PostMapping("/users/"us})
private ResponseEntity<Users> AddUser(@RequestBody User us)
{
	 UserService.save(us);

}


						2