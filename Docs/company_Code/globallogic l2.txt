Employee:- id,name

List<Employee> emp = new ArrayList<>();

List<Employee> result = emp.stream().filter(e->e.getId()!=100))
			.map(e->System.out.println(e->e.setName)).collect(Collectors.toList());



class A
{
@Autowired
private B b;

}

class B
{

@Autowired
private A a;
	
	sysout("in B");
}

