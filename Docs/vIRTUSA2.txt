Optional<String> optional = Optional.empty();
 try{ 
if(optional.isPresent())
{
System.out.println("Using get():" + optional.get()); 
}
else
{
	sysout("Its Empty");
}
catch(Exception e)
{ System.out.println("Caught Exception"); }


String input = "successes";


char mostrepeated = input.chars().mapToObj(c->(char)c).collect(Collectors.groupingBy(c->c,Collectors.counting()). 		    entrySet().stream().sorted(Comparator.comparingInt(Map.Entry::getValue).reversed()).filter(e->e.getValue()>1).map(e->e.getKey()).findFirst());



COUNTRY -- STATUS,ACTIVITY,name


select name,count(status) from country where ACTIVITY LIKE '%PAYMENT%' AND STATUS = 'FAILED' group by name;